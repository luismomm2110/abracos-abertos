const mainDiv = {
    display: "flex",
    justifyContent: "center",
    background: "#005298",
}

const menuLink = {
    color: "#fff",
}

const nav = {
    flexDirection: "row",
    justifyContent: "center",
    alignItems: "center",
    marginBottom: "4px"
}

const navBrand = {
    background: "#fff",
    padding: "0.2em",
    borderRadius: "16px"
}

const styles = {
    mainDiv,
    menuLink,
    nav,
    navBrand
}

export default styles;
